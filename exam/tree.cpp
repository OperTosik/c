#include "tree.h"

Tree::Tree(FILE* f) {
    int x;
    TreeNode* tmp, walk;
    scanf("%d", &x);
        root->value = x;
    for (int i = 0; i < 30; ++i) {
        scanf("%d", &x);
        tmp->value = x;
    }


}


void T_Print(FILE *f, TreeNode *pos) {

}